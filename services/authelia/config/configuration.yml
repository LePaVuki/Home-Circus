server:
  address: 'tcp://0.0.0.0:9091/'

session:
  name: 'authelia_session'
  domain: {$ROOT_DOMAIN}

access_control:
  default_policy: 'deny'
  rules:
    - domain: 'auth.{$ROOT_DOMAIN}'
      policy: 'bypass'
    - domain: '*.{$ROOT_DOMAIN}'
      policy: 'one_factor'